# Conductor environment — copy to ~/.config/clade/conductor.env

# Hearth connection (Kamaji's own API key)
HEARTH_URL=https://54.84.119.14
HEARTH_API_KEY=<kamaji-api-key>
HEARTH_NAME=kamaji

# Worker registry config
CONDUCTOR_WORKERS_CONFIG=/opt/clade/deploy/conductor-workers.yaml

# Worker Hearth API keys (referenced by conductor-workers.yaml)
OPPY_HEARTH_API_KEY=<oppy-api-key>

# Conductor mode: "claude_code" (default) or "api" (Anthropic API agent)
# CONDUCTOR_MODE=claude_code

# Anthropic API key (required when CONDUCTOR_MODE=api)
# ANTHROPIC_API_KEY=<anthropic-api-key>

# Model override (default: claude-haiku-4-5-20251001)
# CONDUCTOR_MODEL=claude-haiku-4-5-20251001
