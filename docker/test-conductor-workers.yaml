# Worker registry for the conductor in the Docker test environment.
# Used by clade-conductor MCP server (Kamaji) to delegate tasks via Ember.
workers:
  testember:
    ember_url: "http://ember:8100"
    ember_api_key: "testkey-ember"
    hearth_api_key: "testkey-ember"
    working_dir: "/home/testuser"
